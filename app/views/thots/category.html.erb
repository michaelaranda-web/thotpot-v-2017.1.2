<h1>Thot#category</h1>
<p>Find me in app/views/thot/category.html.erb</p>

<% @thots.each do |thot| %>
    <div class=<%= "thot-#{thot.id}" %>>
      <h3><%= "(#{thot.id}) #{thot.title}" %></h3>
      <p><%= thot.details %></p>

      <div>
        <%= link_to "Delete", thots_destroy_path(id: thot.id), class: 'delete-button' %>
      </div>
    </div>
    <br>
    ------------
<% end %>